# Eurekan 프로젝트 실행 지식 베이스

## 문서 목적
- 이 문서는 Eurekan 프로젝트의 최상위 실행 규칙이다.
- 이 문서의 모든 내용은 반드시 한글로 유지한다.
- 현재 저장소의 진실은 실제 파일 구조와 실제 코드다.
- 코드가 바뀌면 같은 변경에서 이 문서도 함께 갱신한다.

## 현재 프로젝트 정체성
- 프로젝트명: Eurekan Investment
- 제품 형태: 정적 단일 페이지형 위키/매거진
- 핵심 목표: 주식투자에 도움이 되는 39개 산업·기술·소비·금융 테마를 분야별 장문 페이지로 보여주는 읽기 중심 사이트
- 현재 구현 원칙: 백엔드 없이 `.gitignore`, `_config.yml`, `README.md`, `STOCK.md`, `index.html`, `about.html`, `contact.html`, `privacy.html`, `disclaimer.html`, `style.css`, `app.js`, `data.json`, `robots.txt`, `ads.txt`, `sitemap.xml`, `pages/`, `images/`, `agents/`만으로 동작한다.
- 종목별 심화 리서치 원천은 로컬 전용 `research/` 폴더에 `국가코드_티커_한글종목명.md` 형식으로 저장하되, 이 폴더는 Git 추적과 GitHub Pages 배포 대상에서 제외한다.
- 현재 운영 분야는 아래 39개 투자 테마 분야이며, 이전 broad 분야 체계는 최상위 분야로 사용하지 않는다.

## 현재 파일 구조
```text
AGENTS.md
.gitignore
_config.yml
README.md
STOCK.md
index.html
about.html
contact.html
privacy.html
disclaimer.html
style.css
app.js
data.json
robots.txt
ads.txt
sitemap.xml
pages/
  <slug>.html
images/
  <slug>-220.webp
  <slug>-330.webp
  <slug>-500.webp
  <slug>-960.webp
  <slug>-1280.webp
agents/
  README.md
  <desk>-agent-<name>.md
research/  # 로컬 전용, Git 추적 및 GitHub Pages 배포 제외
  <KR|US>_<ticker>_<한글종목명>.md
```

- 위 구조 외의 프로젝트 파일/폴더는 유지하지 않는다. 단, `research/`는 종목별 심화 리서치 원천을 저장하는 로컬 전용 작업 폴더로 예외 허용한다.
- `README.md`는 공개 저장소용 프로젝트 소개 문서로 유지한다.
- `STOCK.md`는 관련종목 표시에 사용하는 한국·미국 주식 종목명, 티커, 네이버증권 링크 참고 파일로 유지한다.
- `agents/` 폴더는 분야별 전문가 페르소나 문서 저장용으로 남긴다.
- `images/` 폴더는 외부 공개 원본 이미지를 글별 로컬 WebP 후보로 최적화해 저장하는 폴더로 유지한다.
- `research/` 폴더는 종목별 히스토리, 사업 변천, 공시 요약, 자본조달, 경영진 변화, 주가 변곡점, 리스크, 비교 기업, 현재 확인 지표를 누적하는 로컬 전용 지식 베이스로 사용한다.
- `research/` 안의 파일명은 `국가코드_티커_한글종목명.md` 형식을 기본으로 한다. 예를 들어 미국 주식은 `US_NVDA_엔비디아.md`, 한국 주식은 `.KS`, `.KQ` 접미사를 뺀 `KR_005930_삼성전자.md`처럼 저장한다.
- 종목명에 `/`처럼 파일명에 쓸 수 없는 문자가 있으면 `·`처럼 읽기 쉬운 구분 문자로 치환한다.
- `research/`는 `.gitignore`와 `_config.yml`에서 제외해 GitHub 저장소 커밋과 GitHub Pages 배포에 포함하지 않는다.
- 실제 사용자-facing 사이트는 루트의 `index.html`, `about.html`, `contact.html`, `privacy.html`, `disclaimer.html`, `style.css`, `app.js`와 `pages/<slug>.html` 정적 글 페이지를 사용한다.
- `about.html`, `contact.html`, `privacy.html`, `disclaimer.html`은 애드센스 신청과 사이트 신뢰 확보를 위한 루트 정적 안내 페이지로 유지한다.
- 검색엔진 수집 보조 파일은 루트의 `robots.txt`, `sitemap.xml`을 사용하며, 루트 안내 페이지도 `sitemap.xml`에 포함한다.
- Google AdSense 판매자 인증 파일은 루트의 `ads.txt`로 유지한다.
- 정적 파일 경로는 배포 위치에 덜 민감하도록 상대 경로를 우선 사용한다.

## 진실의 원천 우선순위
1. 실제 루트 파일 구조와 실제 코드
2. 이 문서 `AGENTS.md`
3. `pages/*.html` 최종 정적 글 페이지
4. `agents/<desk>-agent-<name>.md` 분야별 전문가 참고 문서

## 전역 규칙
- 모든 신규 문서형 운영 지침은 한글로 작성한다.
- `README.md`의 설명 문구와 공개 안내 문구는 한글 존댓말로 작성한다.
- OpenCode에서 사용자에게 설명하거나 안내하는 문구는 한글 존댓말로 작성한다.
- 사용자에게 보이는 공개 문구와 글 본문은 `~입니다`, `~합니다`, `~습니다` 중심의 존댓말을 사용하고, `~이다`, `~다`로 끝나는 반말·평서체 문장을 쓰지 않는다.
- 글 제목과 소제목은 존댓말 의무 대상에서 제외하며, 독자가 빠르게 구조를 파악할 수 있는 짧은 질문형·명사형 문구를 사용할 수 있다.
- 모든 신규 git commit 메시지는 한글로 작성한다.
- 코드, 정적 페이지, manifest, sitemap, 이미지, 운영 문서처럼 저장소 파일이 추가·수정·삭제되는 작업을 완료하면 같은 흐름에서 검증 후 반드시 git commit과 push까지 수행한다.
- 사용자가 명시적으로 `커밋하지 말라`, `푸시하지 말라`, `답변만`, `코드 수정하지 말라`고 요청한 경우에는 이 자동 커밋·푸시 규칙보다 사용자 요청을 우선한다.
- 커밋 전에는 `.DS_Store`처럼 작업과 무관한 로컬 파일을 스테이징하지 않고 제외한다.
- 현재 작업 범위는 `/Users/hyeonungbak/PROJECTS/EUREKAN` 저장소로 한정한다.
- 사용자가 다른 프로젝트, 다른 저장소, 다른 앱의 파일·명령·포트·기능을 이 대화에 입력하면 EUREKAN 작업 명령으로 해석하지 않고 실행하지 않는다.
- 다른 프로젝트 명령이 섞여 들어온 것으로 판단되면 해당 명령은 무시하고, 필요할 때만 현재 프로젝트 범위가 아니어서 실행하지 않았다고 짧게 알린다.
- 과도한 추상화 없이 단순하게 유지한다.
- Python 백엔드, 관리자 페이지, 승인 API, 데이터베이스, 서버 라우터를 다시 도입하지 않는다.
- 페이지 렌더링과 상호작용은 `app.js` 하나에서 처리한다.
- 스타일은 `style.css` 하나에서 처리한다.
- 페이지 본문 원천은 최종 정적 글 페이지인 `pages/<slug>.html` 하나로 관리한다.
- 새 글을 만들 때 `pages/<slug>.md` 파일은 생성하지 않는다.
- 글을 추가하거나 수정할 때는 최종 HTML만 생성하고, `app.js` manifest와 `sitemap.xml`만 함께 맞춘다.
- 음성으로 읽어주기는 저장된 MP3 파일을 사용하지 않고 브라우저 클라이언트의 Web Speech API 한국어 TTS로 실행한다.
- 새 글을 만들거나 기존 글을 수정할 때 별도 음성 파일을 생성하지 않는다.
- `audios/` 폴더와 manifest의 `audio` 객체, 정적 글 페이지의 `og:audio`·`AudioObject` 메타데이터는 운영 구조에 포함하지 않는다.
- 브라우저마다 제공하는 한국어 음성 품질과 경계 이벤트 정확도는 다를 수 있으므로, `app.js`는 `SpeechSynthesisUtterance` 경계 이벤트와 시간 기반 추정 진행률을 함께 사용한다.
- 페이지 메타데이터 목록은 `app.js` 내부 manifest 배열에서 관리한다.
- 현재 공개 글 수는 `app.js` manifest와 `sitemap.xml` 기준으로 관리하며, 사용자가 콘텐츠 추가를 요청하면 분야별 균형과 품질을 함께 확인한다.
- 여러 글을 한 번에 추가할 때도 글 1개마다 3천자 이상 본문, 대표 이미지, 관련 영상, manifest, sitemap 반영 여부를 개별 검증한 뒤 게시 완료로 본다.
- 사이트 신뢰 페이지인 소개, 문의, 개인정보처리방침, 면책고지는 루트 정적 HTML로 유지하고, 모든 공개 화면의 공통 하단에서 접근할 수 있어야 한다.

## 금지 사항
- React, Vue, Svelte 같은 프레임워크 도입 금지
- Python/Node 백엔드 재도입 금지
- 관리자 승인 플로우 재도입 금지
- 로그인/회원가입/결제 기능 도입 금지
- 페이지 외부에 별도 콘텐츠 저장 포맷 추가 금지

## 사이트 동작 원칙
- 헤더는 스크롤해도 상단에 고정하지 않고 일반 문서 흐름에 둔다.
- 헤더 안에는 가운데 정렬된 `💡 EUREKAN INVESTMENT` 브랜드 제목과 분야별 메뉴만 둔다.
- 홈에서는 `💡 EUREKAN INVESTMENT` 브랜드 제목을 `h1`로 둘 수 있지만, 정적 글 페이지에서는 글 제목을 유일한 `h1`로 두고 헤더 브랜드는 일반 브랜드 링크 요소로 표시한다.
- 헤더 안의 브랜드 제목과 분야별 메뉴는 모두 가운데 정렬한다.
- 분야별 메뉴는 헤더 안에서 브랜드 제목 아래에 두고, 항상 1줄 가로 메뉴로 표시한다.
- 헤더의 분야별 메뉴 라벨은 39개 투자 테마 순서에 맞춰 `1. 인공지능 🤖`처럼 앞에는 순번과 분야명을 두고, 이모티콘은 분야명 뒤에 붙여 표시하되, 라우팅에 사용하는 분야명 값은 기존 원문 분야명으로 유지한다.
- 분야별 메뉴가 화면 폭보다 길어지면 좌우 가로 스크롤로 탐색하게 하되, 메뉴 영역의 스크롤바는 시각적으로 숨긴다.
- 화면 폭이 520px 이하로 매우 좁아지면 분야별 메뉴 버튼을 숨기고 제목 줄 오른쪽에 우측 정렬된 햄버거 버튼으로 대체하며, 햄버거 버튼을 누르면 분야별 메뉴가 흰색 드롭다운 패널로 펼쳐진다.
- 검색창은 헤더 안에 넣지 않고 메인 영역의 최상단에 두며, 헤더와 콘텐츠 사이 간격이 과하게 벌어지지 않도록 상하 여백을 작게 유지한다.
- 검색창 왼쪽에는 현재 공개된 전체 콘텐츠 수를 `총 콘텐츠 70개`처럼 앞자리 0 없이 자연수 형식으로 표시한다.
- 사이트 최상위 배경과 헤더 배경은 흰색 계열로 통일하고, 화면 구분은 배경색 차이가 아니라 얇은 경계선, 부드러운 그림자, 올리브 포인트 색상으로 만든다.
- 사이트 favicon은 `💡` 이모지 기반 SVG 데이터 URI를 사용한다.
- 홈에서는 카드 안에 분야 라벨과 제목만 보여주되, 공개 외부 이미지 URL이 등록된 글은 카드 상단에 해당 이미지를 썸네일로 보여준다.
- 카드 썸네일과 정적 글 본문 이미지는 원본 크기를 그대로 노출하지 않고 표시 영역의 가로 폭에 맞춘 뒤 가로 2:세로 1 비율로 크롭해 보여준다.
- 홈 카드 썸네일은 가능한 경우 원본 이미지 URL을 그대로 쓰지 않고 카드 표시 크기에 맞는 축소 이미지 URL과 `srcset`/`sizes`를 함께 사용해 모바일 전송량을 줄인다.
- 홈 상단 최신 콘텐츠 슬라이더에서는 첫 화면 첫 카드 이미지만 `eager`와 `fetchpriority="high"`로 불러오고, 나머지 슬라이더·섹션 이미지는 `lazy`와 낮은 우선순위로 둔다.
- 홈 초기 렌더링에서는 화면에 렌더된 카드 이미지의 `src`와 `srcset`을 처음부터 넣어 빈 썸네일이 오래 보이지 않게 하고, 로딩 우선순위는 브라우저의 `loading`과 `fetchpriority` 속성으로 조절한다.
- 무한 카드 슬라이더는 끊김 없이 이어질 만큼만 반복 DOM을 만들고, 모바일 PageSpeed를 해치지 않도록 불필요한 중복 카드와 즉시 로드 이미지를 늘리지 않는다.
- 모든 글의 대표 썸네일 이미지는 사이트 전체 기준으로 중복되면 안 되며, 같은 파일 경로, 같은 외부 URL, 같은 생성 결과물, 같은 원본을 리사이즈나 포맷만 바꾼 이미지를 다른 글에 재사용하지 않는다.
- 페이지 카드는 요약 문단을 표시하지 않고 콘텐츠 높이에 맞게 빈 공간 없이 보여준다.
- 화면에 표시하는 분야명은 `app.js`에서 정규화하며, 현재 공개 분야는 39개 투자 테마 분야만 사용한다.
- 이전 운영 분야였던 기술, 반도체, 경제, 글로벌, 정치, 과학, 문화, 스포츠, 크립토, 레시피는 새 투자 정보 구조에서는 공개 분야로 쓰지 않는다.
- 검색은 제목, 요약, 분야를 기준으로 동작한다.
- 카드 클릭 시 검색엔진이 바로 읽을 수 있는 `pages/<slug>.html` 정적 글 페이지로 이동한다.
- 카드 링크는 검색엔진이 읽을 수 있도록 `pages/<slug>.html` 정적 글 페이지를 우선 사용하며, 분야 링크는 `?desk=<분야>` 쿼리 라우팅을 사용한다. 기존 `?page=<slug>`, `#page`, `#desk` 주소도 `app.js`에서 호환한다.
- 정적 글 페이지는 `pages/<slug>.html`에 전체 본문과 글별 SEO 메타데이터를 포함하며, `app.js`는 이 최종 HTML의 본문을 기준으로 상호작용용 상세 화면을 보강한다.
- `app.js`나 `style.css`처럼 모든 페이지에 영향을 주는 공용 자산을 바꾸면 브라우저 캐시 때문에 배포 직후 변경이 안 보일 수 있으므로, 같은 변경에서 `index.html`과 `pages/*.html`의 해당 자산 URL 버전 쿼리를 함께 갱신한다.
- 홈 첫 화면에서 LCP 후보가 되는 대표 이미지는 외부 URL을 직접 로드하지 않고 `images/<slug>-330.webp` 같은 로컬 WebP 후보를 우선 사용하며, `index.html`의 `preload`도 같은 로컬 후보 기준으로 맞춘다.
- 홈 최신 콘텐츠 슬라이더는 시퀀스 래퍼 없이 동일 카드 묶음을 단일 레일 안에 반복 렌더링하되, 한 묶음의 폭이 화면보다 짧아지지 않게 필요한 만큼 카드를 보강하고 렌더 후 첫 카드와 다음 반복 묶음의 첫 카드 사이 실제 거리를 측정해 `--slide-distance`를 설정한다.
- 이미지가 꼭 필요한 글은 공개 외부 원본 이미지를 먼저 고른 뒤, `images/<slug>-220.webp`, `images/<slug>-330.webp`, `images/<slug>-500.webp`, `images/<slug>-960.webp`, `images/<slug>-1280.webp` 로컬 WebP 후보로 변환해 사용한다.
- 새 글의 대표 이미지는 글 내용을 실제로 설명하는 무료 이미지로 우선 고른다. 음식·레시피 글은 텍스트만 있는 생성 SVG나 추상 플레이스홀더를 대표 썸네일로 쓰지 않고, Wikimedia Commons 같은 무료 출처의 실제 음식 사진을 우선 사용한다.
- 외부 무료 이미지를 쓸 때는 `app.js` manifest에 원본 확인용 `externalSrc`, `sourceUrl`, `alt`를 함께 넣고, 정적 글 페이지의 본문 이미지·Open Graph·Twitter 카드·JSON-LD는 같은 글의 로컬 WebP 대표 경로로 맞춘다.
- 상세 본문 렌더링은 최종 HTML의 `#article-body`를 기준으로 하며, 예전 분야 표현은 `app.js`에서 정규화해 보여준다.
- 상세 본문 소제목은 최종 HTML 안의 `<h3 class="article-subheading">` 요소로 표시한다.
- 홈 상단에는 `최신 콘텐츠` 무한 카드 슬라이더가 있다.
- 홈 상단 최신 콘텐츠 제목에는 분야별 최신 대표글 수를 괄호 안 숫자로 함께 표시한다.
- 무한 카드 슬라이더는 카드 단위 반복 레일을 만들고, 첫 카드와 다음 반복의 첫 카드 사이 실제 렌더 거리를 계산해 마지막 카드 뒤에 다음 반복의 첫 카드가 빈 구간 없이 바로 이어지도록 동작한다.
- 홈의 분야별 섹션은 한 줄만 보여주고, 섹션별 페이지네이션과 `+ 더보기` 링크를 제공한다.
- 홈의 분야별 섹션 페이지네이션 숫자 버튼은 한 번에 최대 10개만 표시하고, 10개를 초과하면 `>`와 `<` 버튼으로 다음·이전 묶음으로 이동한다.
- 홈의 분야별 섹션 제목과 분야별 전체 보기 제목에는 해당 분야의 전체 페이지 수를 괄호 안 숫자로 함께 표시한다.
- `+ 더보기`와 헤더 분야 메뉴는 `?desk=<분야>` 라우팅으로 분야별 전체 보기 화면을 열고, 기존 `#desk=<분야>` 주소도 호환한다.
- 상세 화면에는 검색창, 음성으로 읽어주기 버튼, 뉴스레터 신청 UI, 같은 분야 더 보기 슬라이더, 인기 주제 슬라이더가 표시된다. 뉴스레터 신청 UI는 2번째 소제목 바로 아래에, 같은 분야 더 보기 슬라이더는 3번째 소제목 바로 아래에, 관련 영상은 4번째 소제목 바로 아래에 배치한다.
- 모든 새 글은 주제와 직접 연결되는 관련 유튜브 영상 1개를 반드시 선정해 manifest와 정적 HTML에 반영한다.
- 관련 유튜브 영상은 한국어 콘텐츠를 최우선으로 찾고, 주제와 품질이 맞는 한국어 영상이 없을 때만 외국어 콘텐츠를 사용한다.
- 관련 유튜브 영상은 쇼츠, 세로형 짧은 클립, 티저, 하이라이트보다 주제를 충분히 설명하는 롱폼 영상을 우선한다. 기본적으로 5분 이상 분량의 일반 `watch?v=` 형식 영상을 고르고, 유튜브 쇼츠 URL, `isShortsEligible=true`로 확인되는 영상, 3분 미만 영상은 선택하지 않는다.
- 관련 유튜브 영상 후보가 여러 개이면 한국어 콘텐츠 중에서 주제 적합도가 충분하면서 좋아요, 댓글 수, 조회수, 채널 구독자 수 같은 공개 반응 지표가 큰 영상을 우선한다.
- 주제에 맞는 한국어 영상이 없거나 반응 지표가 지나치게 약하면 외국어 영상 중에서 같은 공개 반응 지표와 채널 신뢰도가 높은 영상을 우선한다.
- 유튜브 반응 지표를 직접 확인할 수 없을 때는 공식 채널 여부, 채널 규모, 영상 최신성, 조회수, 댓글 활성도, 재업로드 여부를 대체 기준으로 삼고, 단순히 검색 상단에 보인다는 이유만으로 고르지 않는다.
- 관련 유튜브 영상이 등록된 글은 4번째 소제목이 있으면 4번째 소제목 바로 아래에 `관련 영상` 블록을 표시하고, 4번째 소제목이 없을 때만 상세 본문 하단의 추천 슬라이더 앞에 표시한다.
- 관련 영상은 처음부터 iframe을 로드하지 않고 썸네일과 재생 버튼만 먼저 보여주며, 사용자가 재생 버튼을 누른 뒤에만 `youtube-nocookie.com` iframe을 로드한다.
- 상세 화면과 정적 글 페이지에는 본문 소제목을 모은 목차를 본문 위에 일반 블록으로 표시하며, 목차 링크를 눌렀을 때 라우팅 재렌더링 없이 해당 소제목 위치로 이동해야 한다.
- 특정 글에 관련 상장사가 있으면 `app.js`의 `RELATED_STOCKS_BY_TICKER`, `RELATED_STOCK_TICKERS_BY_DESK`, `RELATED_STOCK_TICKERS_BY_SLUG` 구조를 사용해 최대 4개까지 연결하고, 상세 화면과 정적 글 페이지에서 본문이 끝난 뒤 글 하단에 `관련종목` 블록으로 표시한다.
- 관련종목 표기는 `🇰🇷 종목명` 또는 `🇺🇸 종목명` 형식을 기본으로 하고, 가능하면 티커와 네이버증권 링크를 함께 제공한다.
- 관련종목에서 국기 이모티콘은 종목명 앞에 두고, 한국 주식보다 미국 주식을 먼저 배치한다.
- 한국 종목 티커를 화면에 표시할 때는 `.KS`, `.KQ` 접미사를 노출하지 않고 숫자 코드만 보여준다.
- 관련종목 블록은 본문 밖에 두어 음성 읽기 본문에 포함하지 않는다.
- 관련종목 블록을 동적으로 만들 때는 정적 글 페이지와 홈 상세 화면의 DOM 구조가 다를 수 있으므로, 반드시 `#article-body`와 같은 실제 콘텐츠 부모 안에서 본문 다음 위치에 삽입한다.
- 모든 공개 글은 가능한 경우 `STOCK.md`의 종목명, 티커, 네이버증권 주소를 기준으로 관련종목을 최대 4개까지 연결하며, 개별 페이지 예외가 필요하면 전체 종목 객체를 반복하지 않고 티커 목록만 추가한다.
- 댓글 컴포넌트, 댓글 입력창, 댓글 목록, 댓글 저장 로직은 제공하지 않는다.
- 상세 화면과 정적 글 페이지에서 글 제목과 음성으로 읽어주기 버튼 사이에는 요약 디스크립션이나 설명 문단을 표시하지 않는다.
- 상세 화면과 정적 글 페이지에서 작성자와 발행시각은 음성으로 읽어주기 버튼과 같은 줄에 두고 오른쪽 정렬로 표시하며, `Aiden · 2026.05.31 15:17`처럼 작성자명, 점 구분자, 분 단위 발행시각만 보여준다.
- 음성 버튼과 플레이바가 들어 있는 도구 줄이 스크롤 중 sticky 플로팅 상태로 붙으면 작성자와 발행시각 문구는 숨기고, 음성 버튼과 플레이바만 표시한다.
- 정적 글 페이지의 대표 이미지는 첫 번째 본문 소제목 바로 아래에 로컬 WebP 경로로 표시하고, 캡션에는 원본 확인 페이지를 `출처 : 도메인 주소` 형식으로 표기한다.
- manifest의 `summary`는 검색과 SEO 메타데이터용으로만 사용하고 상세 화면 본문 UI에는 표시하지 않는다.
- 음성 기본 버튼 문구는 `음성으로 읽어주기`, 재생 중 버튼 문구는 `음성읽기 중지하기`로 표시한다. 같은 버튼을 다시 누르면 현재 위치를 보존한 뒤 브라우저 TTS 큐를 취소하고, 다시 누르면 처음부터가 아니라 저장 위치부터 새 TTS를 즉시 시작해야 한다. 브라우저의 `speechSynthesis.pause()`/`resume()`은 재개 지연이 길 수 있으므로 버튼 이어듣기에는 사용하지 않는다.
- 음성 버튼 오른쪽에는 항상 보이는 재생 위치 플레이바를 두고, 사용자가 플레이바를 클릭하거나 드래그하면 해당 위치부터 다시 읽을 수 있게 한다.
- 음성 버튼과 플레이바가 들어 있는 도구 줄은 글 본문을 아래로 스크롤해도 계속 접근할 수 있도록 정적 글 페이지와 상세 화면에서 sticky 플로팅 바처럼 따라다니게 한다.
- 플레이바는 본문 텍스트의 문자 위치를 기준으로 갱신하며, 사용자가 재생 지점을 옮기면 선택 위치가 포함된 문장 단위부터 Web Speech API 읽기를 다시 시작한다.
- 음성 읽어주기는 글 제목을 읽지 않고 첫 본문 문단부터 바로 읽는다.
- 브라우저 TTS 기반 음성 읽기는 본문을 문장 단위로 나눠 각 문장 전체를 하나의 `SpeechSynthesisUtterance`로 재생한다. 단어 단위 utterance, 문장 여러 개를 묶는 utterance, 다중 사전 큐잉은 Safari와 Chrome에서 재개 위치가 어긋나거나 첫 단어 뒤 멈춤을 만들 수 있으므로 사용하지 않는다.
- 음성 읽기를 중지했다가 다시 시작하면 추정 문자 위치가 아니라 중지 당시 재생 중이던 문장의 시작점부터 다시 읽는다.
- 브라우저가 긴 한국어 TTS 중간에 무음 상태로 빠지는 것을 막기 위해 `onend` 이벤트가 오면 다음 문장을 하나씩 이어 붙이고, `onend` 이벤트가 오지 않을 때는 예상 재생 시간보다 짧은 여유만 둔 fallback 타이머로 다음 음성 단위 재생을 빠르게 복구한다.
- 음성 재생 중에는 진행 위치가 포함된 현재 문장 전체를 형광색으로 표시하고, 일시 중지·중지·완료 시에는 형광 표시를 제거한다. 하이라이트는 `onstart` 이벤트를 기다리기 전에 현재 재생 큐에 올라간 문장 기준으로 즉시 표시해야 한다.
- 본문 로딩 중/읽는 중/읽기 완료 상태 문구는 별도로 표시하지 않는다.
- 음성 읽어주기 본문에는 이미지 캡션, 출처, 뉴스레터 신청 UI, 관련 영상 블록, 같은 분야 더 보기 슬라이더, 인기 주제 슬라이더 텍스트를 포함하지 않는다.
- 홈, 정적 글 페이지, 루트 안내 페이지 하단에는 `소개`, `문의`, `개인정보처리방침`, `면책고지` 링크를 포함한 공통 푸터를 둔다.
- 공통 푸터에는 모든 투자 관련 글이 일반 정보이며 매수·매도 추천이나 투자 자문이 아니라는 짧은 고지를 표시한다.

## 종목 리서치 누적 워크플로우
- 종목 리서치 파일은 Eurekan Investment의 핵심 차별점으로 취급한다. 공개 글은 단발성 요약이 아니라 `research/`에 축적된 종목별 히스토리, 숫자, 오해, 리스크, 비교 관점을 바탕으로 점점 더 깊어져야 한다.
- 새 글을 작성하거나 기존 글을 크게 수정할 때 관련종목이 1개 이상 연결되는 경우, 글 작성 전에 반드시 해당 종목의 `research/국가코드_티커_한글종목명.md` 로컬 리서치 파일을 먼저 확인한다.
- 관련종목 후보는 `STOCK.md`, `app.js`의 관련종목 매핑, 글 주제의 핵심 기업, 분야별 대표 상장사를 함께 보고 최대 4개까지 고른다.
- 모든 글의 주제는 관련종목의 주주들이 가장 많이 놓치고 있지만 현재 가장 궁금해할 질문을 우선으로 정한다. 단순 기업 소개, 일반 산업 전망, 이미 널리 알려진 호재 반복보다 `주주가 지금 확인하고 싶지만 자료를 끝까지 읽지 않으면 알기 어려운 지표·리스크·전환점·오해`를 글의 중심 질문으로 삼는다.
- 사용자가 `각 분야마다 글 1개씩`, `분야별 글 추가`, `랜덤 분야 글 추가`처럼 여러 글을 요청한 경우에도 각 글마다 아래 순서를 반복한다.
- 리서치 파일이 없으면 `STOCK.md`의 시장, 종목명, 티커, 네이버증권 링크를 기준으로 `research/국가코드_티커_한글종목명.md` 파일을 먼저 만든 뒤 진행한다.
- 글 작성 전 리서치 점검 순서는 `관련종목 확정 → research 파일 열기 → 빈 항목 확인 → 오래된 확인일 확인 → 출처 없는 주장 분리 → 공식 공시·IR·실적 자료·신뢰 가능한 뉴스로 보강 → 글 작성용 관점 정리` 순서로 진행한다.
- 미국 주식은 SEC EDGAR, 회사 IR, 10-K, 10-Q, 실적 발표 자료, 거래소 공식 정보를 우선 확인하고, 한국 주식은 DART, KIND, 회사 IR, 사업보고서, 분기보고서, 네이버증권을 우선 확인한다.
- 리서치 파일 안에서 공시·IR로 확인한 내용은 `검증된 사실`에 적고, 투자 해석이나 글의 관점은 `분석 메모` 또는 `글 작성용 관점`에 따로 적어 사실과 해석을 섞지 않는다.
- 리서치 파일의 각 핵심 정보에는 가능한 한 `값`, `출처`, `확인일`, `신뢰도`, `다음 확인 필요일`을 함께 남긴다.
- 글을 작성한 뒤에는 사용한 핵심 판단 근거, 새로 확인한 지표, 글에서 연결한 페이지 경로를 해당 종목 리서치 파일의 `연결된 EUREKAN 글`과 `업데이트 로그`에 기록한다.
- 글 작성 플로우는 `사용자 요청 → 분야/주제 확정 → 관련종목 선택 → 종목 리서치 파일 확인 → 부족 정보 보강 → 리서치 관점 업데이트 → 글 작성 → 사용 근거 재기록 → pages HTML 생성 → app.js manifest 갱신 → sitemap.xml 갱신 → 검증 → 커밋/푸시` 순서로 본다.
- 글 작성 중 사용자가 리서치 갱신 여부를 확인하더라도 `답변만` 또는 `코드 수정하지 말라`는 요청이 없다면 작업을 멈추지 않고 리서치 보강과 글 발행 흐름을 계속 이어간다.
- `research/`는 로컬 전용 지식 베이스이므로 GitHub Pages 배포와 커밋 대상에 포함하지 않는다. 단, 리서치 운영 규칙이 바뀌면 이 문서 `AGENTS.md`는 함께 갱신하고 커밋한다.
- 리서치 보강이 오래 걸려 전체 글 작성 흐름을 막을 때는 관련종목별로 최소한 `마지막 확인일`, `이번 글에서 사용한 핵심 지표`, `출처 URL`, `글 작성용 관점`, `업데이트 로그`만 먼저 채우고, 나머지는 `추가 확인 필요` 상태로 남긴다.

## 글 작성 과정 공유 규칙
- 사용자가 새 글 작성, 기존 글 재작성, 분야별 글 추가, 랜덤 분야 글 추가를 요청하면 실제 작성과 파일 수정에 들어가기 전에 글쓰기 플로우를 사용자에게 순차적으로 설명한다.
- 글쓰기 플로우 설명은 단순 계획이 아니라 이번 글에 실제로 적용할 선택 과정을 보여주는 작업 로그여야 한다.
- 글 1개마다 최소한 아래 항목을 순서대로 설명한다.
- `주제 선정`: 어떤 분야와 어떤 투자 질문을 글의 중심 주제로 잡았는지, 그 질문이 관련종목 주주들이 잘 모르지만 현재 궁금해할 만한 내용인지, 왜 그 주제가 현재 사이트와 독자에게 맞는지 설명한다.
- `관련종목 선정`: `STOCK.md`, 분야 기본 매핑, 주제 직접 관련 기업, 종목 리서치 파일을 기준으로 어떤 관심종목을 최대 4개까지 골랐는지와 제외한 후보가 있다면 이유를 설명한다.
- `리서치 확인`: 어떤 `research/국가코드_티커_한글종목명.md` 파일을 확인하거나 갱신할지, 어떤 공식 공시·IR·실적 자료·신뢰 가능한 뉴스가 필요한지 설명한다.
- `참고자료 선정`: 본문에 사용할 핵심 참고자료의 종류와 URL, 각 자료에서 확인할 숫자나 사실을 먼저 설명한다. 최신 정보가 필요한 주제는 웹검색 또는 공식 자료 확인을 생략하지 않는다.
- `대표 이미지 선정`: 어떤 장면을 썸네일로 삼을지 먼저 문장으로 확정하고, 어떤 이미지 후보를 찾았는지, 라이선스와 원출처가 왜 적합한지 설명한다.
- `관련 유튜브 선정`: 한국어 롱폼 영상을 우선 검색하고, 쇼츠 제외 여부, 주제 적합성, 채널 신뢰도, 조회수·댓글·구독자 등 공개 반응 지표를 어떤 기준으로 봤는지 설명한다. 한국어 후보가 약하면 외국어 롱폼을 선택한 이유를 함께 설명한다.
- `글 구조 설계`: 도입부의 문제의식, 3~4개 소제목, 각 소제목에서 다룰 핵심 숫자·사례·판단 기준을 먼저 보여준다.
- `작성·반영 순서`: 본문 작성, 정적 HTML 생성, `app.js` manifest, 관련종목 매핑, 이미지 WebP 후보, `sitemap.xml`, 캐시 버전 쿼리, 검증, 커밋·푸시 순서를 설명한다.
- 여러 글을 한 번에 작성할 때도 전체 요약만 하고 넘어가지 않고, 글 1개마다 위 항목을 짧게라도 반복해 설명한다.
- 작업 도중 선택이 바뀌면 변경 이유를 사용자에게 다시 알린다. 예를 들어 더 적합한 이미지나 영상, 더 직접적인 관련종목, 더 신뢰도 높은 참고자료를 찾은 경우 기존 후보와 교체 이유를 설명한다.
- 최종 답변에는 작성 완료된 글의 slug, 분야, 관련종목, 대표 이미지 출처, 유튜브 영상, 주요 참고자료, 검증 결과, 커밋·푸시 여부를 요약한다.

## SEO 규칙
- 기본 SEO 메타 태그는 `index.html`의 `<head>`에 둔다.
- Google Search Console 소유권 확인용 `google-site-verification` 메타 태그는 홈페이지 `index.html`의 `<head>` 안에 유지한다.
- Google AdSense 자동 광고 스크립트는 홈, 루트 안내 페이지, 모든 정적 글 페이지의 `<head>` 안에 유지한다.
- Google AdSense 사이트 연결용 `google-adsense-account` 메타 태그는 홈, 루트 안내 페이지, 모든 정적 글 페이지의 `<head>` 안에 유지한다.
- Google AdSense용 `ads.txt`에는 `google.com, pub-2425701206768506, DIRECT, f08c47fec0942fa0` 레코드를 유지한다.
- 라우트별 제목, 설명, canonical, Open Graph, Twitter 카드, JSON-LD 구조화 데이터는 `app.js`에서 현재 화면 상태에 맞게 갱신한다.
- 정적 글 페이지의 본문 제목은 검색엔진이 페이지 주제를 바로 파악할 수 있도록 `h1#article-title`로 표시하고, 브랜드명이나 숨겨진 목록 제목이 글의 최상위 제목 신호를 대신하지 않게 한다.
- 홈은 `WebSite`, 분야별 전체 보기는 `CollectionPage`, 상세 글은 `Article` JSON-LD를 사용한다.
- 이미지가 등록된 정적 글은 Open Graph, Twitter 카드, `Article` JSON-LD의 `image` 메타데이터를 함께 제공한다.
- 모든 새 글은 `Article` JSON-LD의 `subjectOf`에 `VideoObject`를 포함하고, Open Graph `og:video` 메타데이터는 `youtube-nocookie.com` 임베드 URL 기준으로 갱신한다.
- 존재하지 않는 문서는 `noindex, follow`로 표시한다.
- canonical URL은 실제 배포 주소 `https://lifeiz0415.github.io/EUREKAN/`를 기준으로 만들고, 상세 글은 `pages/<slug>.html`, 분야 보기는 `?desk=<분야>`를 사용한다.
- `robots.txt`와 `sitemap.xml`은 루트에 둔다.
- `sitemap.xml`은 실제 배포 주소 `https://lifeiz0415.github.io/EUREKAN/` 기준의 홈, 루트 안내 페이지, 분야, 정적 글 페이지 URL을 담는다.
- manifest에 글을 추가하거나 slug를 바꾸면 같은 변경에서 `pages/<slug>.html`과 `sitemap.xml`도 함께 갱신한다.

## 사이트 신뢰와 애드센스 준비 규칙
- 애드센스 신청 전후로 `about.html`, `contact.html`, `privacy.html`, `disclaimer.html`은 삭제하지 않는다.
- `about.html`은 사이트 목적, 콘텐츠 범위, 운영 방식을 독자가 이해할 수 있게 설명한다.
- `contact.html`은 오류 제보, 저작권, 개인정보, 광고 관련 문의 채널을 명확히 안내한다.
- `privacy.html`은 뉴스레터 이메일 수집 항목, 이용 목적, 보관·삭제 요청 방법, 외부 이미지 요청, Google 광고 쿠키와 개인 맞춤 광고 가능성을 함께 고지한다.
- `disclaimer.html`은 모든 투자 관련 글이 매수·매도 추천이나 투자 자문이 아니라는 점을 별도 페이지로 안내한다.
- 뉴스레터 UI, Google 광고 코드, 외부 분석 도구, 새로운 제3자 스크립트를 추가하거나 변경하면 같은 변경에서 `privacy.html`과 공통 푸터 고지를 함께 검토한다.
- 애드센스 코드가 발급되어 삽입될 때는 사이트 전체 `<head>` 적용 범위, 개인정보처리방침의 광고·쿠키 고지, 검색엔진 메타데이터 충돌 여부를 함께 확인한다.

## 뉴스레터 규칙
- 이 사이트의 상세 화면 상호작용은 뉴스레터 신청만 제공한다.
- 뉴스레터 신청 UI는 메인 목록이 아니라 각 상세 페이지에만 노출한다.
- 뉴스레터 신청 UI는 본문과 관련종목을 모두 읽은 뒤 접근할 수 있도록 글 하단에 배치한다.
- 뉴스레터 신청 UI는 상세 제목/설명 문구를 표시하지 않고, 이메일 입력창과 신청 버튼만 보여준다.
- 뉴스레터 이메일 입력창 placeholder는 `귀하의 이메일 주소로 최신 무료 뉴스레터를 신청하세요 ( you@email.com)`로 표시한다.
- 뉴스레터 신청 시 사용자는 관심 분야를 직접 고르지 않는다.
- 사용자가 보고 있는 페이지의 정규화된 분야가 곧 관심 분야다.
- `data.json`의 `newsletter_subscribers` 배열에 이메일과 관심 분야를 기록한다.
- 같은 이메일이 다른 분야 페이지에서 다시 신청되면 `interests` 배열에 분야를 누적한다.
- 브라우저가 File System Access API를 지원하면 연결된 `data.json` 파일에 직접 저장한다.
- 직접 저장이 불가능하면 업데이트된 `data.json` 다운로드를 생성해 사용자가 파일을 교체하도록 한다.

## data.json 규칙
- `data.json`은 최소한 아래 구조를 가진다.
- 뉴스레터 구독 데이터 외의 사용자 입력 데이터는 `data.json`에 저장하지 않는다.

```json
{
  "newsletter_subscribers": [],
  "updated_at": "ISO-8601"
}
```

- 각 구독자는 아래 구조를 따른다.

```json
{
  "email": "user@example.com",
  "interests": ["경제"],
  "last_page_slug": "us-china-tariff-war-2026",
  "subscribed_at": "ISO-8601",
  "updated_at": "ISO-8601"
}
```

## pages 폴더 규칙
- 각 페이지는 최종 정적 HTML인 `pages/<slug>.html` 파일 하나로 유지한다.
- `pages/<slug>.md`는 더 이상 생성하거나 유지하지 않는다.
- 기존 마크다운 원본이 남아 있더라도 운영 원천으로 취급하지 않으며, 새 작업에서는 HTML 본문을 기준으로 수정한다.
- 토픽 페이지 파일명은 `분야-hot-topic-번호` 형식을 쓰지 않고, 글 제목을 영어로 요약한 kebab-case slug를 사용한다.
- slug는 제목의 핵심 명사와 독자 질문을 3~8개 영어 단어로 압축해 만든다.
- slug에는 가능하면 정규화된 39개 테마 분야를 떠올릴 수 있는 영어 접두어(`artificial-intelligence`, `cloud-computing`, `electric-vehicle`, `semiconductor`, `fintech`, `crypto-blockchain` 등)를 앞에 둔다.
- slug는 소문자 영문, 숫자, 하이픈만 사용하고 공백, 한글, 특수문자, 마침표는 넣지 않는다.
- slug 끝에는 글의 시의성을 구분할 수 있도록 필요한 경우 `2026` 같은 연도 토큰을 붙인다.
- `hot-topic`, `topic`, `issue`, 번호만 붙인 이름, 의미가 빈약한 임시 이름은 파일명으로 쓰지 않는다.
- 제목이 비슷해 slug가 겹치면 단순 번호를 먼저 붙이지 말고 제목의 차이를 드러내는 영어 핵심어를 추가해 구분한다.
- slug를 정하면 같은 slug로 `pages/<slug>.html`, `app.js`의 manifest 항목, 정적 HTML 내부 canonical/OG/JSON-LD URL, `sitemap.xml`을 모두 함께 갱신한다.
- 한 파일은 최소 3천자 이상의 장문 본문을 가진다.
- 분야와 메타데이터는 파일 안이 아니라 `app.js`의 manifest에서 관리하고, 생성된 HTML의 head 메타데이터도 이 manifest를 기준으로 맞춘다.
- 글 발행시각은 날짜만 쓰지 않고 `YYYY-MM-DDTHH:mm:ss+09:00` 형식으로 시분초와 시간대까지 기록한다.
- 글 발행시각은 임의로 생성하지 않고, 해당 글의 본문·정적 HTML·manifest·sitemap 반영이 모두 끝난 최종 생성 시점의 실제 한국시간 년월일시분초를 정확히 입력한다.
- 실제 최종 생성 시점보다 미래인 발행시각을 가진 글은 예약 글로 간주하며, 해당 시각 전에는 목록·검색·상세 화면·sitemap에서 공개하지 않고 정적 글 페이지도 `noindex, follow` 상태로 둔다.
- 새 글을 처음 생성할 때는 본문과 별개로 해당 글 내용과 직접 연결되는 대표 이미지까지 같은 작업에서 반드시 확정한다.
- 새 글 대표 이미지를 고르거나 기존 글에 썸네일을 추가할 때는 반드시 해당 글의 `pages/<slug>.html` 첫 본문 문단, 소제목, 핵심 사례를 먼저 읽고 이미지 장면을 정한다.
- 새 글이나 기존 글에 관련종목이 연결되어 있으면 대표 썸네일 후보를 고를 때 해당 관련종목의 기업·상품·서비스·사업장·공장·장비·운송수단·데이터센터·매장·공식 행사처럼 글 내용과 종목을 동시에 설명하는 공개 사용 가능 이미지를 먼저 찾는다.
- 새 글 대표 이미지는 같은 분야라는 이유만으로 공용 이미지를 재사용하지 않고, 해당 글의 제목·요약·첫 본문 문단·소제목이 가리키는 구체적 장면이나 대상에 맞는 이미지로 고른다.
- 새 글 대표 이미지는 이미 게시된 다른 글과 같은 이미지를 다시 쓰지 않는 것을 원칙으로 하며, 부득이하게 비슷한 대상을 다루더라도 다른 이미지 파일을 선택한다.
- 새 글 대표 이미지는 저작권 문제가 없는 공개 사용 가능 이미지여야 하며, 라이선스와 핫링크 가능 여부를 확인한 뒤 사용한다.
- 새 글 대표 이미지는 외부 원본 URL을 그대로 노출하는 데서 끝내지 않고, 같은 변경에서 `images/<slug>-220.webp`, `images/<slug>-330.webp`, `images/<slug>-500.webp`, `images/<slug>-960.webp`, `images/<slug>-1280.webp` 후보를 모두 생성한다.
- 새 글은 대표 이미지가 `app.js` manifest의 `image` 객체, `images/<slug>-*.webp` 로컬 후보, `pages/<slug>.html` 본문 이미지, Open Graph, Twitter 카드, JSON-LD `image`, 출처 캡션까지 함께 반영되기 전에는 게시 완료로 보지 않는다.
- 상세 화면과 정적 글 페이지는 글 제목 아래 도구 줄에서 음성으로 읽어주기 버튼과 같은 줄 오른쪽에 해당 분야의 순환 에이전트 이름과 발행시각을 표시한다.
- 본문은 기사문/블로그문 형식의 읽기 쉬운 한국어 존댓말 문단으로 작성한다.
- 본문은 온전히 해당 주제의 장면, 이해관계, 비용, 변화, 판단 기준만 다루며 글을 쓰는 에이전트·작성자·페르소나의 관점이나 문체를 직접 설명하지 않는다.
- `Ada는`, `Iris는`, `작성자는`, `이 에이전트는`, `문체는`, `관점에서는`, `자료의 문장을 흉내 내지 않습니다`처럼 내부 작성 방식이나 페르소나 설정을 독자에게 드러내는 문장은 본문에 쓰지 않는다.
- 에이전트별 개성은 본문 밖에서 설명하지 않고, 문장 리듬·비유·질문 순서·사례 선택의 차이로만 자연스럽게 드러낸다.
- 글은 공통 템플릿을 채운 흔적이 보이지 않게 주제별 고유 장면과 용어를 중심으로 개별 작성하며, 여러 글에 같은 문단 구조와 문장을 반복하지 않는다.
- 동일한 도입부, 동일한 설명 문장, 동일한 결론 문단, 동일한 소제목 세트가 여러 글에 반복되는 페이지는 애드센스 심사와 검색 품질에 맞지 않는 반복 콘텐츠로 보고 게시 목록, sitemap, `pages/`에서 제거한다.
- `이 문서는 그 신호가 어디서 나왔고`, `중요한 것은 이 흐름을 하나의 유행어로 소비하지 않고`, `앞으로의 관전 포인트는 세 가지입니다`, `좋은 위키 문서는 복잡한 변화를 독자의 일상 언어로`처럼 여러 글에 그대로 들어갈 수 있는 범용 문장은 본문에 쓰지 않는다.
- 본문 문장 끝은 `~입니다`, `~합니다`, `~습니다`처럼 독자에게 설명하는 존댓말을 기본으로 하며, `~이다`, `~다`로 끝나는 반말·평서체 문장을 피한다.
- 한 문단이 지나치게 길어지지 않도록 HTML의 `<p>` 단위로 자연스럽게 구분한다.
- 각 글은 첫 본문 문단을 도입부로 두고, 그 이후 흐름을 기준으로 3~4개의 `<h3 class="article-subheading">` 소제목을 배치한다.
- 소제목은 반복 템플릿을 쓰지 않고, 이어지는 문단 묶음의 핵심 명사·긴장·판단 기준을 압축한 고유 문구로 작성한다.
- 소제목은 본문 문장과 달리 `~입니다`, `~합니다`, `~나요` 같은 존댓말로 끝낼 필요가 없으며, `누가 비용을 감당하나`, `무엇이 출하를 막나`, `신뢰는 어디서 깨지나`처럼 짧고 자연스러운 질문형을 우선한다.
- 소제목 중복을 피하려고 slug의 영문 단어를 그대로 뒤에 붙이지 않는다. `inference`, `tools`, `fandom` 같은 영문 꼬리표 대신 글 제목에서 뽑은 자연스러운 한국어 핵심어로 구분한다.
- `왜 지금 주목받나`, `변화가 작동하는 방식`, `남은 제약과 부담`, `앞으로 확인할 기준`, `왜 지금 커졌나`, `변화가 움직이는 방식`, `부담이 남는 지점`, `앞으로 볼 기준`처럼 여러 글에 그대로 반복될 수 있는 범용 소제목은 쓰지 않는다.
- `전환점`, `작동 조건`, `남은 부담`, `확인 신호`, `검증 기준`, `확산 조건`처럼 딱딱한 고정 꼬리말을 붙여 만든 소제목을 반복하지 않는다.
- 소제목은 표지판 같은 명사구보다 독자가 다음 문단을 읽고 싶어지는 질문형·장면형 문구를 우선한다.
- 좋은 소제목은 `누가 비용을 감당하나`, `무엇이 출하를 막나`, `신뢰는 어디서 깨지나`처럼 문단 안의 갈등과 장면이 보이게 쓴다.
- 소제목은 문단 첫 문장을 그대로 복사하지 말고, 이어지는 문단 묶음에서 독자가 흥미를 느낄 핵심 질문과 판단 기준을 8~18자 안팎의 자연스러운 한국어로 요약한다.
- 한 글 안의 소제목 3~4개는 서로 다른 핵심어를 사용해 문단 전개가 보이게 만들고, 같은 분야의 다른 글과도 같은 4개 세트가 반복되지 않게 한다.
- 소제목에는 콜론식 부제 구조, 번호 나열, 과장 수식어, `결론적으로`, `종합하면`, `한마디로`, `요컨대` 같은 공식적 마무리 표지어를 쓰지 않는다.
- 소제목은 `pages/<slug>.html` 본문에 `<h3 class="article-subheading">` 형태로 직접 반영한다.
- 제목에는 AI 생성 문장처럼 보이는 콜론식 부제 구조를 쓰지 않고, 필요한 의미를 자연스러운 한국어 제목 문장으로 풀어 쓴다.
- 마지막 문단은 `결론적으로`, `결국`, `요컨대`, `정리하면`, `종합하면`, `한마디로` 같은 공식적 마무리 표지어로 시작하지 않고, 글의 실제 함의와 다음 판단 기준을 자연스럽게 남기며 끝낸다.

## 분야별 페이지 생성 플로우 규칙
- 자동 토픽 문장 배열에서 대량으로 페이지를 생성하는 방식은 반복 콘텐츠를 만들 수 있으므로 사용하지 않는다.
- 현재 `topicPages`는 반복 콘텐츠 재발행을 막기 위해 빈 배열로 유지한다.
- `hotTopicsByDesk`, `topicSlugsByDesk`, `topicImagesBySlug` 같은 자동 토픽 생성용 배열과 매핑은 유지하지 않는다.
- 새 글은 자동 대량 생성이 아니라 글 1개 단위로 기획, 작성, 이미지 선정, 관련 유튜브 영상 선정, 정적 HTML 생성, manifest 반영, sitemap 반영, 검증을 끝낸 뒤 게시한다.
- 사용자가 여러 개의 글 작성을 요청하면 한 번에 묶어 처리하지 않고, 글 1개마다 분야 에이전트 선택, 주제 확정, slug 확정, 본문 작성, 이미지 선정, 관련 유튜브 영상 선정, 정적 HTML 생성, manifest 반영, sitemap 반영, 검증 루틴을 순서대로 반복한다.
- 같은 분야의 글은 해당 분야 전담 에이전트가 맡는다. 한 분야 안에서 글 수가 늘어나 문체 반복 위험이 커지면 같은 분야 안에 보조 에이전트를 추가할 수 있지만, 최상위 분야명은 39개 목록을 유지한다.
- 게시가 완료되려면 manifest 항목과 같은 slug의 `pages/<slug>.html` 정적 글 페이지, 그리고 `sitemap.xml` URL이 반드시 함께 존재해야 한다.
- 사용자가 별도로 요청하지 않는 한 분야별 글 수가 과도하게 치우치지 않게 관리하며, 새 글을 추가할 때는 같은 분야의 중복·저품질 글이 있는지 함께 점검한다.
- 상단 `최신 콘텐츠` 슬라이더는 전체 게시 목록에서 분야별 최신 발행 페이지 한 개씩을 가져와 구성한다.

## app.js 구현 규칙
- 페이지 manifest 배열이 있어야 한다.
- 각 항목은 최소 `slug`, `title`, `desk`, `summary`를 가진다. 대표 이미지는 웹검색으로 찾은 공개 사용 가능 외부 이미지를 원본으로 삼되, 사용자에게 표시되는 이미지는 `images/<slug>-*.webp` 로컬 후보를 우선 사용한다. manifest의 `image` 객체는 원본 추적과 출처 표기를 위해 `image.externalSrc`, `image.alt`, `image.sourceUrl`, 필요한 경우 `image.width`, `image.height`를 함께 가진다. 기존 호환을 위해 `image.src`도 사용할 수 있으나, 새 글에서는 외부 이미지 URL을 `externalSrc`에 두는 것을 우선한다.
- 새 글의 manifest 항목은 반드시 `video` 객체를 가진다. `video` 객체는 `youtubeId`, `title`, `channel`, `sourceUrl`, 필요한 경우 `description`, `thumbnailUrl`, `uploadDate`를 함께 가진다.
- `video.youtubeId`는 11자 유튜브 ID로 저장하는 것을 우선하며, `sourceUrl`은 `https://www.youtube.com/watch?v=<id>` 형식의 원본 보기 URL로 둔다.
- 관련 영상은 한국어 콘텐츠를 먼저 검색하고, 주제 적합도와 신뢰도가 충분한 한국어 영상이 없을 때만 외국어 콘텐츠를 보조 후보로 검토한다.
- 관련 영상은 주제와 직접 연결된 공식 채널, 공공기관, 언론사, 기업 IR·발표 채널, 분야 전문가 채널을 우선하며, 단순 자극적 해설 영상이나 출처가 불분명한 재업로드 영상은 사용하지 않는다.
- 관련 영상 후보에서 쇼츠, 세로형 1분 내외 클립, 제목만 자극적인 짧은 편집본은 제외하고, 본문 주제의 배경·근거·전망을 이해할 수 있는 롱폼 영상을 우선한다. 가능하면 실제 영상 페이지의 `lengthSeconds`와 `isShortsEligible` 값을 확인해 5분 이상·비쇼츠 영상인지 검증한 뒤 사용한다.
- 관련 영상 선정 시 한국어 후보를 먼저 비교하고, 후보별 좋아요, 댓글 수, 조회수, 채널 구독자 수를 확인할 수 있으면 그 지표를 기록 기준으로 삼는다. 지표 확인이 제한되면 채널 권위, 공개일, 조회수, 댓글 활성도, 영상 설명의 출처 명확성을 함께 비교한다.
- 외국어 영상을 선택할 때도 같은 방식으로 좋아요, 댓글 수, 조회수, 채널 구독자 수 또는 대체 신뢰 지표를 비교하며, 한국어 영상보다 주제 적합도와 반응 지표가 명확히 나은 경우에만 사용한다.
- 각 항목의 `publishedAt`은 `YYYY-MM-DDTHH:mm:ss+09:00` 형식을 사용한다.
- 현재 게시 manifest는 `featuredPages`를 주 원천으로 사용하며, 반복 콘텐츠 재발행 방지를 위해 `topicPages`는 빈 배열로 유지한다.
- 자동 토픽 생성용 `hotTopicsByDesk`, `topicSlugsByDesk`, `topicImagesBySlug`, `deskSlugs`는 다시 추가하지 않고, 화면 표시와 뉴스레터 저장값은 `normalizeDesk()` 기준이어야 한다.
- 홈 카드 렌더링, 검색 필터, 정적 글 페이지 본문 보강, 뉴스레터 저장은 모두 `app.js`에서 처리한다.
- `app.js`는 `pages/*.md`에 의존하지 않고, 필요한 경우 `pages/<slug>.html`에서 `#article-body`를 읽어 상호작용용 상세 화면을 구성한다.
- `app.js`는 manifest의 `slug`를 기준으로 `images/<slug>-1280.webp` 대표 이미지와 카드·본문용 `srcset`을 자동 구성하며, 외부 이미지 URL은 로컬 후보가 없는 비상 fallback과 출처 확인용으로만 사용한다.
- `app.js`는 manifest의 `video` 객체가 있는 글에 관련 영상 블록을 추가하고, 4번째 소제목이 있으면 해당 소제목 바로 아래로 이동시키며, 영상 재생 버튼을 눌렀을 때만 `youtube-nocookie.com` iframe을 생성한다.
- 라우트별 SEO 메타데이터 갱신도 `app.js`에서 처리한다.
- 카드 슬라이더의 실제 이동 거리 계산은 `refreshSliderLoops()`에서 처리한다.
- 목록/상세 화면 전환, 상세 문맥 초기화, 뉴스레터 패널 초기화처럼 여러 라우트에서 반복되는 UI 상태 변경은 `setMainView()`, `clearArticleContext()`, `resetNewsletterPanel()` 같은 공유 헬퍼를 우선 사용한다.
- 외부 라이브러리 없이 바닐라 JavaScript만 사용한다.

## style.css 구현 규칙
- 전역 스타일 파일은 `style.css` 하나만 유지한다.
- 전역 배경과 헤더 배경은 흰색으로 맞추고, 패널·카드·공통 푸터는 흰색 면 위에 낮은 대비의 경계선과 그림자로만 층위를 만든다.
- 홈 헤더는 브랜드명을 첫 줄에 두고, 분야별 메뉴를 같은 헤더 안의 다음 줄에 1줄 가로 스크롤 메뉴로 둔다.
- 분야별 메뉴의 좌우 스크롤 동작은 유지하되, `scrollbar-width: none`과 WebKit 스크롤바 숨김 스타일로 화면에 스크롤바가 보이지 않게 한다.
- `app.js`는 `520px` 이하 화면에서 `is-desk-menu-collapsed` 클래스를 적용해 햄버거 버튼 모드로 전환하고, 그보다 넓은 화면에서는 햄버거 버튼을 숨긴 뒤 분야별 메뉴를 즉시 보여준다.
- 검색창은 메인 영역 최상단에 별도 검색 바 영역으로 배치하되, `.layout`의 상단 여백과 검색창 아래 간격은 기본 12px 수준으로 유지한다.
- 검색창과 전체 콘텐츠 수 표시는 같은 `search-bar` 안에서 가로로 배치하고, 좁은 화면에서는 한 줄 배치가 깨지지 않도록 자연스럽게 세로로 접히게 한다.
- 카드 상단에는 분야 라벨이 표시된다.
- 뉴스레터 UI는 상세 페이지에만 표시하며, 2번째 소제목이 있으면 해당 소제목 바로 아래로 이동시킨다.
- 뉴스레터 박스는 제목/설명 제거 상태에 맞게 상하 여백을 같은 값으로 유지하며, 빈 상태 메시지는 공간을 차지하지 않는다.
- 관련 영상 블록은 `.article-video` 계열 스타일로 관리하고, 16:9 비율의 썸네일·iframe 영역이 모바일과 데스크톱에서 레이아웃을 밀거나 겹치지 않게 한다.
- 루트 안내 페이지는 `.legal-page` 스타일을 사용해 장문 고지를 읽기 좋게 배치한다.

## 종목 이슈 분야 규칙
- 개별 기업·종목을 다루는 글은 특정 종목만 고정해서 반복하지 않고 거래대금, 실적, 공시, 수주, 고객 변화, 규제 변화가 함께 커진 대상을 우선 후보로 삼는다.
- 기업 글은 해당 기업의 공식 IR, 공시, 실적 자료, 거래소 자료를 기준으로 매출 구조, 마진, 현금흐름, 고객 집중, 밸류에이션을 확인한다.
- 암호화폐·블록체인 글은 거래소 공지, 프로젝트 공식 문서, 블록체인 익스플로러, 토큰 언락 자료, 현물·선물 거래대금, 펀딩비, 온체인 사용량을 함께 확인한다.
- 신규상장 종목 정보는 상장일, 공모가 또는 상장가, 초기 유통량, 락업·보호예수 해제 일정, 주요 주주 또는 재단·팀 물량, 첫 실적 또는 온체인 검증 항목, 거래소 입출금 가능 여부를 함께 제공한다.
- 종목 이슈 글은 정보 제공과 판단 기준 정리를 목적으로 하며 매수·매도 추천, 목표가 제시, 수익률 약속, 단정적 투자 조언을 쓰지 않는다.

## 레시피 분야 규칙
- 레시피 분야의 글은 메뉴명만 소개하지 않고 재료 준비, 손질 순서, 불 조절, 간 조정, 완성 상태, 실패 복구법, 보관과 재가열 기준을 함께 다룬다.
- 레시피 분야 에이전트 문서에는 `재료`와 `조리법`을 필수 구성 요소로 명시하고, 새 레시피 글을 만들 때 `재료`는 반드시 `구분`, `재료`, `분량`, `준비 기준` 4열 표 형식으로, `조리법`은 단계와 상태 기준이 보이도록 본문에 드러나야 한다.
- 레시피 글 제목은 메뉴명과 독자가 가장 자주 실패하는 조리 질문을 함께 담아야 하며, `쉬운`, `초간단`, `무조건 성공`처럼 과장된 표현을 남발하지 않는다.
- 레시피 본문은 독자가 계량만 따라 하는 글이 아니라 색, 향, 농도, 소리, 재료의 숨이 죽는 정도처럼 조리 중 확인 가능한 상태 기준을 제공해야 한다.
- 단백질, 해산물, 보관 음식처럼 안전과 연결되는 메뉴는 충분한 익힘과 보관 기준을 빠뜨리지 않되, 검증되지 않은 건강 효능이나 치료 효과를 주장하지 않는다.
- 레시피 대표 이미지는 완성 음식과 직접 연결되는 실제 음식 사진을 우선하며, 같은 요리라도 이미 다른 글에 사용한 이미지 URL이나 같은 원본의 리사이즈 이미지를 재사용하지 않는다.

## 이미지 규칙
- 이미지가 꼭 필요한 정적 글은 먼저 웹검색으로 주제와 맞는 저작권 문제가 없는 무료 외부 이미지 URL을 찾고, 직접 생성·관리하는 이미지 파일은 만들거나 저장하지 않는다.
- 이미지 선정 로직의 최우선 기준은 각 글의 실제 내용과 직접 연결되는 장면성이다. 분야명, 제목 키워드, slug만으로 이미지를 배정하지 않고, 본문이 설명하는 핵심 사건·대상·행위·이해관계·비용·판단 기준을 분석한 뒤 그 글에만 어울리는 이미지를 고른다.
- 해당 글에 `관련종목` 블록이 있거나 manifest의 관련종목 매핑이 있으면 이미지 검색 순서는 `본문 핵심 장면 → 관련종목의 실제 사업 장면 → 분야 일반 이미지` 순서로 둔다. 관련종목 이미지는 단순 로고, 주가 차트, 워터마크 이미지보다 제품·시설·서비스 사용 장면처럼 독자가 종목과 투자 포인트를 함께 이해할 수 있는 후보를 우선한다.
- 이미지 작업은 품질과 저작권 안정성을 위해 아래 `웹검색 우선 모드`를 기본 플로우로 삼는다.
- `웹검색 우선 모드`는 글 1개마다 `제목·slug 확인 → pages/<slug>.html 본문 핵심 3줄 추출 → 첫 본문 문단과 소제목 확인 → 이미지 장면 한 문장 확정 → 공개 사용 가능 외부 이미지 웹검색 → 라이선스·원출처·핫링크 가능성 확인 → app.js/정적 HTML 메타데이터 치환 → sitemap.xml 갱신 → 빠른 검증 → 커밋·푸시` 순서로 처리한다.
- 앞으로 정치 분야 대표 이미지와 본문 이미지를 새로 고르거나 교체할 때는 인물 사진, 사람 얼굴이 선명하게 드러나는 사진, 특정 정치인을 중심에 둔 이미지를 선택하지 않는다. 정치 글에는 투표 도구, 공공기관 건물, 의회·회의장 전경, 문서, 예산·정책을 상징하는 사물처럼 사람 얼굴이 드러나지 않는 이미지를 우선한다.
- 개별 기업·종목·암호화폐 글의 대표 이미지는 기본적으로 로고·텍스트 합성 없이 공개 사용 가능한 실제 이미지를 우선한다. 사용자가 명시적으로 로고형 썸네일을 원해도 새 이미지를 생성하지 않고, 공식 또는 공개 라이선스 이미지 중에서 선택한 뒤 로컬 WebP 후보로 최적화한다.
- 빠른 검증은 최소 `node --check app.js`, 정적 HTML JSON-LD 파싱, `git diff --check`를 포함한다. 화면 깨짐이 의심되거나 레이아웃·이미지 로딩을 바꾼 경우에는 브라우저 검증까지 추가한다.
- 여러 글을 연속 처리할 때도 본문 분석과 이미지 장면 확정은 글 1개씩 순차적으로 수행한다. 다만 검증과 커밋은 사용자가 허용하면 3~5개 단위로 묶을 수 있다.
- 외부 이미지는 저작권과 원출처, 검색엔진 접근성을 확인한 뒤 사용하며, 실제 화면에서는 로컬 WebP 후보를 우선 로드한다.
- 외부 이미지는 독자가 보는 이미지 안에 텍스트, 숫자, 로고, 워터마크가 없는 후보를 우선한다.
- 외부 이미지는 글 제목이나 분야만 보고 대충 고르지 않고, 해당 글의 핵심 사건·장소·제품·행위·이해관계자를 먼저 좁힌 뒤 그 장면에 맞는 후보를 선택한다.
- 외부 이미지는 이미 다른 글에 쓴 동일 URL을 다시 쓰지 않는 것을 원칙으로 하며, 새 글마다 다른 이미지 파일을 배정한다.
- 외부 이미지 후보가 여러 개일 때는 저작권이 더 명확하고, 글 내용과 연결이 더 직접적이며, 카드 썸네일로 잘 보이는 후보를 우선한다.
- 외부 이미지의 `sourceUrl`은 가능하면 썸네일 파일 주소가 아니라 저작자, 원출처, 라이선스를 확인할 수 있는 파일 설명 페이지 또는 공식 원출처 페이지를 사용한다.
- 라이선스, 원출처, 저작자 정보가 불분명한 이미지는 주제 적합도가 높아도 사용하지 않는다.
- 이미지를 추가하면 같은 변경에서 `app.js`의 manifest 이미지 정보, `images/<slug>-*.webp` 후보, 카드 썸네일, 정적 HTML 본문 이미지, 출처 캡션, Open Graph 이미지, Twitter 이미지, JSON-LD `image`, 접근성용 `alt`를 함께 갱신한다.
- `app.js` 매니페스트에는 `thumbnail` 메타데이터를 넣지 않고, 이미지가 필요한 경우 `image` 객체만 사용한다. 웹검색 이미지는 `externalSrc`와 `sourceUrl`을 우선 사용한다.

## agents 폴더 규칙
- `agents/`는 분야별 전문가 페르소나 문서용으로만 유지한다.
- 실제 런타임 코드와 연결되지 않는다.
- 기능별 페르소나는 따로 두지 않고, 탐색·수집·작성·검증·편집·정책 점검·유지보수 역할을 각 분야 전문가 에이전트가 통합 수행한다.
- 새 페이지를 만들 때는 해당 분야의 순환 차례에 맞는 `agents/<desk>-agent-<name>.md`를 참고한다.
- 사용자가 특정 분야나 주제에 대한 페이지, 콘텐츠, 글 작성 등 유사한 생성 요청을 하면 먼저 `agents/` 안의 분야별 전문가 문서 중 가장 유사한 분야의 에이전트 페르소나를 선택한다.
- 분야가 명시되지 않았거나 여러 분야가 겹치면 주제의 핵심 독자 질문, 주요 이해관계자, 영향 영역을 기준으로 가장 가까운 분야를 고르고, 그 에이전트 페르소나의 담당 범위·중점 관점·페이지 생성 절차에 입각해 페이지를 생성한다.
- 현재 에이전트는 39개 투자 테마 분야별 전담 에이전트 39명이다.
- 인공지능 분야는 Aiden가 담당한다.
- 클라우드 컴퓨팅 분야는 Nimbus가 담당한다.
- 전기차 분야는 Volt가 담당한다.
- 운송·물류 분야는 Harbor가 담당한다.
- 우주 기술 분야는 Orion가 담당한다.
- 재생에너지 분야는 Sol가 담당한다.
- 전통 에너지 분야는 Flint가 담당한다.
- 반도체 분야는 Silica가 담당한다.
- 사이버보안 분야는 Cipher가 담당한다.
- 산업 자동화 분야는 Gear가 담당한다.
- 로보틱스 분야는 Axis가 담당한다.
- 나노기술 분야는 Nano가 담당한다.
- 사물인터넷 분야는 Mesh가 담당한다.
- 에너지 원자재 분야는 Quarry가 담당한다.
- 농업·식품 분야는 Terra가 담당한다.
- 소프트웨어 분야는 Kernel가 담당한다.
- 양자컴퓨팅 분야는 Qubit가 담당한다.
- 방산·항공우주 분야는 Vector가 담당한다.
- 통신 분야는 Signal가 담당한다.
- 자율 시스템 분야는 Pilot가 담당한다.
- 금속 원자재 분야는 Forge가 담당한다.
- 환경 지속가능성 분야는 Grove가 담당한다.
- 하드웨어 분야는 Circuit가 담당한다.
- 가상·증강현실 분야는 Holo가 담당한다.
- 전자상거래 분야는 Cart가 담당한다.
- 디지털 엔터테인먼트 분야는 Studio가 담당한다.
- 소비재 분야는 Shelf가 담당한다.
- 스마트홈 분야는 Hearth가 담당한다.
- 헬스케어·바이오테크 분야는 Genome가 담당한다.
- 핀테크 분야는 Ledger가 담당한다.
- 생체인식 분야는 Biokey가 담당한다.
- 빅데이터 분야는 Delta가 담당한다.
- 소셜미디어 분야는 Pulse가 담당한다.
- 부동산·리츠 분야는 Brick가 담당한다.
- 웨어러블 분야는 Band가 담당한다.
- 에듀테크 분야는 Tutor가 담당한다.
- 고령화 분야는 Silver가 담당한다.
- 건강식품 분야는 Nutri가 담당한다.
- 암호화폐·블록체인 분야는 Chain가 담당한다.

- 새 분야 desk를 추가할 때는 글을 먼저 쓰지 않고 `app.js`의 39개 분야 목록·이모티콘·작성자 등록, `agents/README.md` 목록 갱신, `agents/<theme>-agent-<name>.md` 생성, `AGENTS.md` 규칙 갱신을 먼저 끝낸 뒤 글을 작성한다.
- 현재 39개 분야는 각 1명의 전담 에이전트로 운영한다. 새 투자 테마 분야를 추가할 때는 기존 분야와 중복되지 않는지 먼저 확인하고, 필요하면 39개 목록 자체를 함께 갱신한다.
- 새 에이전트 파일명은 `<theme-slug>-agent-<english-name>.md` 형식의 kebab-case를 사용하고, 각 분야의 이름·역할·문체 인상이 서로 구분되어야 한다.
- 신규 에이전트 페르소나 문서는 기존 에이전트 문서와 같은 수준으로 `역할`, `담당 범위`, `필체 개성`, `참고 웹사이트`, `벤치마킹 글쓰기 템플릿`, `문장형 템플릿`, `문단형 템플릿`, `문체 학습 지침`, `통합 역할`, `분야별 중점 관점`, `페이지 생성 절차`, `금지 사항`, `완료 기준` 섹션을 모두 포함한다.
- 신규 에이전트의 `참고 웹사이트`는 최소 3개를 두며, 각 항목은 단순 URL 나열이 아니라 그 사이트에서 어떤 관점, 자료 구조, 검증 습관을 참고할지 한글 문장으로 설명한다.
- 참고 웹사이트를 벤치마킹할 때는 원문 표현, 제목 리듬, 고유 비유, 문장 배열을 모방하지 않고 도입 방식, 자료 확인 순서, 주장 전개, 반론 처리, 마무리 방식만 추상화해 새 한국어 템플릿으로 변환한다.
- 신규 에이전트는 벤치마킹 템플릿을 여러 개 보유해야 하며, 각 템플릿은 도입 방식, 글 구조, 설명 방식, 설득 방식, 마무리 방식을 분리해 기록한다.
- 신규 에이전트의 문장형 템플릿 수는 문단형 템플릿 수의 4배 이상이어야 한다. 현재 기본값은 문장형 32개와 문단형 8개이며, 문단형 템플릿이 늘어나면 문장형 템플릿도 비율에 맞춰 함께 늘린다.
- 신규 에이전트의 템플릿 제목, 비유 축, 질문 방식, 전개 순서, 마무리 방식은 기존 전체 에이전트 문서와 중복되면 안 된다.
- 신규 에이전트 템플릿을 생성한 뒤에는 같은 분야의 다른 에이전트뿐 아니라 전체 에이전트 문서와 비교해 동일하거나 거의 같은 제목·문장 구조·문단 구조가 없는지 검증한다.
- 신규 에이전트 문서가 충분한 참고 웹사이트와 템플릿을 갖추기 전에는 해당 desk의 새 글을 게시 완료로 보지 않는다.
- 같은 분야 안에서도 각 에이전트는 서로 다른 필체 개성을 가져야 하며, 문장 리듬, 설명 순서, 비유 사용, 분석 초점에서 차이를 만든다.
- 각 에이전트는 자기 문서의 참고 웹사이트에서 관점, 구성 방식, 검증 습관을 참고하되 원문 문체나 표현을 모방하지 않고 자기 페르소나의 한국어 문체로 변환한다.
- 각 에이전트 문서에는 `벤치마킹 글쓰기 템플릿` 섹션을 두고, 참고 웹사이트에서 관찰한 글 구조, 레이아웃, 주장 전개, 설명 방식, 설득 방식, 도입 방식, 마무리 방식을 문장 모방 없이 추상화해 기록한다.
- 새 글을 작성할 때는 해당 에이전트의 벤치마킹 템플릿 중 하나를 무작위 또는 순환 방식으로 고른 뒤, 주제에 맞게 변형해 사용한다.
- 벤치마킹 템플릿은 독자에게 보이는 본문에 직접 노출하지 않으며, 본문에는 `이 글은 어떤 템플릿을 따릅니다`, `참고사이트처럼`, `에이전트의 관점에서는` 같은 제작 과정 설명을 쓰지 않는다.
- 템플릿은 글의 뼈대만 제공하며, 실제 문단은 해당 주제의 고유 장면, 이해관계자, 비용, 사례, 판단 기준으로 매번 새로 작성한다.
- 각 에이전트 문서에는 실제 글을 조립할 때 활용할 수 있는 `문장형 템플릿`과 `문단형 템플릿` 섹션을 별도로 둔다.
- 문장형 템플릿은 도입 문장, 전환 문장, 대비 문장, 설득 문장, 판단 기준 문장, 마무리 문장처럼 한두 문장 단위로 재조합 가능한 표현 구조를 담는다.
- 문단형 템플릿은 도입부, 배경 설명, 이해관계자 분석, 비용·위험 분석, 반론·한계, 판단 기준, 마무리처럼 한 문단 단위로 재조합 가능한 전개 구조를 담는다.
- 문장형·문단형 템플릿은 그대로 복사해 붙이는 완성 문장이 아니라, 주제별 명사·장면·이해관계자·지표를 갈아 끼워 새 문장으로 변형하는 조립식 설계도다.
- 각 에이전트는 자기 페르소나만의 중독성 있는 리듬, 질문 방식, 비유 방식, 설득 순서, 마무리 여운을 가진 템플릿을 다수 보유해야 하며, 템플릿 수와 다양성이 곧 페르소나 품질의 핵심이다.
- 서로 다른 에이전트의 문장형·문단형 템플릿은 제목, 문장 구조, 비유, 전개 순서, 마무리 방식이 중복되면 안 된다.
- 새 템플릿을 추가할 때는 같은 분야 안의 다른 에이전트뿐 아니라 전체 57개 에이전트 문서와 비교해 동일하거나 거의 같은 문장 구조가 없는지 확인한다.
- 글 작성 시 에이전트는 자기 문서의 문장형·문단형 템플릿을 여러 개 조합하되, 최종 공개 본문에는 템플릿명·페르소나명·참고사이트·작성 방식이 드러나지 않게 자연스러운 장문으로 녹인다.
- 새 글 작성 시 같은 분야의 다른 에이전트와 문장 리듬, 설명 순서, 비유 사용, 분석 초점이 겹치지 않도록 차별점을 먼저 정한다.
- 필체 개성은 `AGENTS.md`의 제목·본문·소제목·SEO 규칙보다 우선하지 않는다.
- `app.js`의 작성자 표시와 정적 HTML의 `article-meta`, JSON-LD `author.name`은 같은 작성자 규칙을 따른다. 각 글은 manifest의 `author`를 명시해 정적 HTML과 동일하게 맞춘다.
- 파일명은 kebab-case를 유지한다.

## 개발 원칙
- 새 기능을 추가할 때 먼저 이 구조를 깨지 않는지 확인한다.
- 단일 정적 앱이라는 현재 제약을 우선한다.
- 사용자가 구조 축소를 원하면 새 디렉터리를 늘리지 않는다.
- 파일 수를 늘리기보다 현재 파일 안에서 해결하는 쪽을 우선한다.
- 코드 변경이 있는 작업은 완료 전에 `AGENTS.md`를 실제 파일 구조와 실제 코드에 맞게 갱신했는지 확인한다.
- 코드 변경이 있는 작업을 완료하면 변경사항을 의미 있는 단위로 한글 커밋 메시지를 사용해 git commit하고 GitHub 원격 저장소에 push한다. 단, 사용자가 해당 작업에서 커밋 또는 푸시 금지를 명시한 경우에는 그 지시를 우선한다.

## 완료 기준
- 루트에는 `AGENTS.md`, `README.md`, `index.html`, `about.html`, `contact.html`, `privacy.html`, `disclaimer.html`, `style.css`, `app.js`, `data.json`, `robots.txt`, `ads.txt`, `sitemap.xml`, `pages/`, `agents/`만 남아 있어야 하며, 글별 HTML은 `pages/` 안에 둔다.
- 홈 카드와 상세 보기, 상세 페이지 뉴스레터 신청이 동작해야 한다.
- `pages/` 안의 각 문서는 장문이어야 한다.
- 소개, 문의, 개인정보처리방침, 면책고지 페이지는 홈과 모든 글 페이지의 공통 푸터에서 접근 가능해야 한다.
- 문서와 실제 코드가 서로 충돌하지 않아야 한다.
